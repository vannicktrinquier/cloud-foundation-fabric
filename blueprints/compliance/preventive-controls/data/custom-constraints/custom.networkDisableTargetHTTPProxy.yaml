custom.${prefix}networkDisableTargetHTTPProxy:
  condition: |-
    true == true
  description: Ensure Target HTTP Proxy are not used
  resource_types:
  - compute.googleapis.com/TargetHttpProxy
  action_type: DENY
  display_name: Deny usage and creation of Target HTTP Proxy
  method_types:
  - CREATE
  - UPDATE
