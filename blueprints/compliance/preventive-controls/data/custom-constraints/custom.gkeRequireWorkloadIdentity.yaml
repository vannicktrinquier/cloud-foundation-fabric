custom.${prefix}gkeRequireWorkloadIdentity:
  condition: |-
    resource.workloadIdentityConfig.workloadPool == ""
  display_name: Enable Workload Identity
  description: Enforce that GKE clusters are enabled with Workload Identity
  resource_types:
  - container.googleapis.com/Cluster
  action_type: DENY
  method_types:
  - CREATE
  - UPDATE
